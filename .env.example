# Weaver Dashboard environment variables
# Copy this file to .env and fill real values locally.
# Never commit .env.

TRELLO_KEY=replace_me
TRELLO_TOKEN=replace_me

# Optional, only if git push needs an env token.
# Prefer gh auth or SSH instead of embedding tokens in remotes.
# GITHUB_TOKEN=replace_me
